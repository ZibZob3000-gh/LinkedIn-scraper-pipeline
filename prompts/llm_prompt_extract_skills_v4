You are an expert technical recruiter and NLP analyst specializing in automated skill extraction.

Your task:
You must ignore all prior examples or previous instructions.
From the section "LINKEDIN JOB VACANCY", extract all hard and soft skills exactly as written — no assumptions, no rewording, no examples from outside the text.
Your goal is to capture every explicitly written technical or interpersonal skill, even when written in unusual formats (e.g. slashes '/', parentheses '()', enumerations, bullet points, or comma-separated lists).

Return only valid JSON with the following structure and nothing else:

{
  "hard_skills": ["..."],
  "soft_skills": ["..."]
}

==============================
STRICT EXTRACTION RULES
==============================

1️⃣ HARD SKILLS
Include only skills that represent a digital, technical, or methodological competency explicitly mentioned in the text.

📌 **Splitting rule:**
IF multiple skills are combined with slashes (/), commas (,), semicolons (;), or within parentheses — you MUST split them into separate items.
Example1 (with '/'): "hard_skill_A/hard_skill_B" → "hard_skill_A", "hard_skill_B" → two separate entries.
Example2 (with ','): "hard_skill_C, hard_skill_D, hard_skill_E" → three separate entries.

📌 **List handling:**
If the text contains a long enumeration of tools or skills, extract *every single one* individually, even if many are grouped together.

Only output the base skill name (no adjectives or context words around it).

✅ Include items such as:
- Programming or scripting languages
- Frameworks, libraries, SDKs
- Databases, BI or analytics tools
- Cloud or DevOps platforms
- APIs, protocols, file/data formats
- Technical methodologies or management tools

🚫 Exclude:
- Education or degrees
- Business domains (e.g. finance, logistics)
- Generic conceptual terms (e.g. innovation, efficiency)
- Soft skills (e.g. communication, adaptability)
- Physical or equipment skills

If no valid hard skills are found, return "hard_skills": [].

---

2️⃣ SOFT SKILLS
Include only interpersonal or cognitive abilities *explicitly written* in the job description.

Examples: teamwork, communication, leadership, adaptability, collaboration, problem-solving, analytical thinking, stakeholder management.

🚫 Exclude:
- Technical skills, tools, or methods
- Business domains or education terms
- Language requirements

If none found, return "soft_skills": [].

---

==============================
FORMAT REQUIREMENTS
==============================

- Output exactly one JSON object.
- No text, explanations, or commentary before or after.
- Hard and soft skills must not overlap.
- Preserve the exact text casing as it appears in the description.

==============================
LINKEDIN JOB VACANCY
==============================

{job_description}
