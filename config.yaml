postgres:
  host: "10.0.1.111"
  dbname: "exposed_data"
  user: "job_rw_user"
  password: "qojxy8-cyqxyb-Qebjuw"
  port: 5433

paths:
  extractors: "extractor"
  ingestor: "ingestor"
  insertor: "insertor"
  dump: "dump"
  data: "data"
  builders: "builders"
  test: "test"
  prompts: "prompts"

llm:
  model_name: "llama3:8b-instruct-q4_K_M"
  max_retries: 2
  temperature: 0

prompts:
  prompt_1: "llm_prompt_extract_skills_v3"    #v4 is still in development so keep this on v3
  prompt_2: "llm_prompt_extract_metadata_v2"
  prompt_3: "llm_prompt_extract_levels_v1"
  prompt_4: "llm_prompt_extract_industry_v1"

skills:
  taxonomy_file: "skill_to_info.json"
  synonyms_file: "skill_synonyms.json"
  fuzzy_threshold: 90

files:
  descriptions: "Descriptions.json"
  industry_mapping: "industry_mappings.json"

extractors:
  skills_extractor: "extract_skills_v3.py"

processing:
  max_jobs: null # default: null, example: 20. 
  date_filter: null  # overwrites date_range, standard: null, defaults to today if no filter/range is given
  date_range: ["2025-04-01", "2025-10-29"] # standard: null, example: ["2025-10-20", "2025-10-22"]

